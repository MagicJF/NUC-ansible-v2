- name: add ssh key for simone
  authorized_key:
    user: simone
    key: 'ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAINrDxZPkqsmmXMDzfBP1Rz3csJUcTPpF2iPvLIA/x/f/ ansible'

- name: generate sshd_config file from template
  tags: ssh
  template:
    src: '{{ ssh_template_file }}'
    dest: /etc/ssh/sshd_config
    owner: root
    group: root
    mode: 0644
  notify: restart_sshd

- name: install ifconfig y tree y curl y wget y git y blabla
  tags: ifconfig
  package:
    name:
      - '{{ ifconfig }}'
      - '{{ tree }}'
      - '{{ curl }}'
      - '{{ wget }}'
      - '{{ git }}'
      - '{{ htop }}'
      - '{{ neofetch }}'
    state: latest
# A implementar
# Copiar carpeta volums docker
# personalitzar bash  https://www.youtube.com/watch?v=Lf7NGZd30uw
# la merda aquella del optional en internet (nomes si es ubuntu-server)
